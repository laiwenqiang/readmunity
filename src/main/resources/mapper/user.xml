<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="entity.User">
    <select id="selectUserById" parameterType="String" resultType="com.readmunity.entity.User">
        SELECT * from user WHERE id = #{id}
    </select>
    
    <select id="selectUserByName" parameterType="String" resultType="com.readmunity.entity.User">
        SELECT name, password FROM user WHERE name = #{name}
    </select>

    <select id="selectUserByEmail" parameterType="String" resultType="com.readmunity.entity.User">
        SELECT name, email FROM user WHERE email = #{email}
    </select>
    
    <insert id="insertUser" parameterType="Map">
        INSERT INTO user (name, email, password) VALUES (#{name}, #{email}, #{password})
    </insert>

    <cache/>

</mapper>